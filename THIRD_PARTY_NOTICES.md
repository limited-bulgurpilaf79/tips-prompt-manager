# Third-Party Notices

## Runtime

TIPS Prompt Manager uses only Python standard-library modules at runtime:
`tkinter`, `sqlite3`, `hashlib`, `hmac`, `json`, `secrets`, `ctypes`, `time`,
`datetime`, `pathlib`, `os`, and `sys`.

Python is distributed under the Python Software Foundation License.

## Build Tool

Windows release binaries are built with PyInstaller. PyInstaller is licensed
under GPL-2.0-or-later with a bootloader exception that permits distribution of
generated executables. PyInstaller source code is not copied into this project.
